@model RoleTOP_MVC.ViewModels.RespostaViewModel
<main>
        <div class="resumo">

            <div>
                <p class="titulo-resumo">@ViewData["Action"] foi realizado.</p>
            </div>

            <div>
                <p class="info-cadastro"><i class="far fa-hand-spock"></i>@Model.Mensagem</p>
            </div>

            <div class="btn">
                <a href='@Url.Action("Index","Home")'><button>Ir para a Home</button></a>
            </div>

        </div>
        <p class="divisoria-rodape"></p>
</main>
